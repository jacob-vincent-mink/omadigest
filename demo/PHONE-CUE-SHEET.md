# OmaDigest sub-minute cue

**Hyper = Super + Ctrl + Alt + Shift**

**Hyper + S = START RECORDING · Hyper + Q = STOP RECORDING**

## Preferred: automated sizzle

From the repository, run `./demo/run-demo.sh 9` once. Do not interact with workspace 9. The runner records only useful moments, waits off camera for real model work, rejects a ≥60s cut, restores your state, and prints the finished file.

On-screen order: notifications → focus complete → digest → Read tab → source catalog → GitHub categories → suggested template → manual/agent template editing → model plan → installed template → PR #482 notification → PR #482 report.

## Manual fallback: core story

`Hyper + P` → wait for “OmaDigest demo ready”.

`Hyper + S` → `Hyper + N` → wait for popups → `Hyper + F` → `Hyper + H` → `Hyper + R` → wait for “Focus session complete” → `Hyper + Q`.

Off camera: wait for the purple unread badge.

`Hyper + S` → `Hyper + V` → hold 5s → `Hyper + E` → hold 1s → `Hyper + Q`.

`Hyper + S` → `Hyper + T` → hold 4s → `Hyper + D` → hold 1s → `Hyper + Q`.

Off camera: wait for the model-authored checklist.

`Hyper + S` → hold 4s on the checklist → `Hyper + Q`.

Off camera: wait for the completed draft.

`Hyper + S` → hold 5s → `Hyper + A` → wait for saved state → hold 2s → `Hyper + Q`.

`Hyper + U` → `Hyper + S` → `Hyper + J` → wait for popup → `Hyper + G` → hold 1s → `Hyper + Q`.

Off camera: wait for the purple unread badge.

`Hyper + S` → `Hyper + V` → hold 6s on the PR-specific title, citations, and **Send to agent** action → `Hyper + Q`.

`Hyper + X` → confirm DND is off.
